#include <stdio.h>

int main() 
{
    int n,i=1,sum=0;
    printf("enter n");
    scanf("%d",&n);
    while(i<=n)
    {
        sum=sum+i;
       i++;
    }
    printf("the sum of n natural numbers is%d",sum);

    return 0;
}
